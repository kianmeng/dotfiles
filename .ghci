-- Better Prompt: Shows loaded modules in a clean format
:set prompt "\ESC[1;32mλ> \ESC[m"

-- Enable multi-line input (useful for pasting code)
:set +m

-- Show the types of expressions after evaluation
:set +t

-- Useful language extensions for interactive play
:set -XOverloadedStrings
:set -XTypeApplications
:set -XScopedTypeVariables

-- Performance: Use -O0 for faster loading in REPL
:set -O0

-- Common imports (available in every session)
import Data.List     -- list processing
import Data.Maybe    -- handle optional values
import Control.Monad -- combinators like when, forever, and void
import Debug.Trace   -- print value with function signature to IO

-- Custom command: :cls to clear the screen
-- Use ":!cls" for Windows
:def cls \_ -> return ":!clear"
